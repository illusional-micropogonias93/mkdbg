Phase 1a note

This directory owns bringup configuration inputs consumed by the host tooling.

Phase 1a keeps the manifest flow stable while mkdbg-native is being split into
multiple source files. Configuration shape is intentionally unchanged so host
tests and generated docs remain compatible.
