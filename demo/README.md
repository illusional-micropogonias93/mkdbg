Phase 1a note

This directory contains small VM32 demo inputs and generated sample artifacts
used by documentation and smoke flows.

Phase 1a keeps these fixtures stable so mkdbg-native and VM32 host refactors do
not change the expected demo paths referenced by scripts and docs.
