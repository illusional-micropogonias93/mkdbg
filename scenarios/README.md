Phase 1a note

This directory stores VM32 scenario sources used by regression and demo flows.

Phase 1a keeps scenario names and file locations stable so host-side tooling can
be reorganized without changing scenario lookup behavior.
