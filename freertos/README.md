Phase 1a note

This directory vendors the FreeRTOS kernel and local RTOS configuration used by
the firmware build.

Phase 1a does not change kernel behavior here. The active work is limited to
host-side mkdbg-native structure and build wiring so RTOS inputs remain stable.
